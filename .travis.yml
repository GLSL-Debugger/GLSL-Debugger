language: cpp
compiler:
    - gcc
    - clang
before_install:
    - sudo add-apt-repository -y ppa:kalakris/cmake
    - sudo add-apt-repository -y ppa:dns/gnu
    - sudo apt-get update -qq
install:
    - sudo apt-get install cmake
    - sudo apt-get install bison=2.7-0gnu1
before_script:
    - mkdir build
    - cd build
    - cmake ..
script: make
branches:
    only:
        - master
notifications:
    email:
        on_success: change
        on_failure: always
    irc:
        channels:
            - "chat.freenode.net#glsl-debugger"
        template:
            - "%{repository}/%{branch} (%{commit} - %{author}): %{message}"
os:
    - linux
